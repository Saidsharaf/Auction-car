import 'package:flutter/material.dart';

class CompanyRegister extends StatelessWidget {
  const CompanyRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
    child: const Center(
      child: Text("Company Register Form"),
    ),
    );
  }
}