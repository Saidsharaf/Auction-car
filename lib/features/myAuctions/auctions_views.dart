import 'package:flutter/material.dart';

class MyAuctions extends StatelessWidget {
  const MyAuctions({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Auctions View'),
      ),
    );
  }
}