import 'package:flutter/material.dart';

class RunningOrdersTab extends StatelessWidget {
  const RunningOrdersTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Running Orders Tab"),
    );
  }
}